cjs-module-mutation/main.ts:3:1 - error SC2020: 'assignment to module.loaded' is part of the standard library types but has no scriptc lowering yet

  2 | 
  3 | module.loaded = true;
    | ^~~~~~~~~~~~~
  4 | module.id = 'replacement';

  hint: CommonJS module metadata is read-only in static builds; assign application state to a separate binding

cjs-module-mutation/main.ts:4:1 - error SC2020: 'assignment to module.id' is part of the standard library types but has no scriptc lowering yet

  3 | module.loaded = true;
  4 | module.id = 'replacement';
    | ^~~~~~~~~
  5 | module.paths[0] = '/tmp/node_modules';

  hint: CommonJS module metadata is read-only in static builds; assign application state to a separate binding

cjs-module-mutation/main.ts:5:1 - error SC2020: 'assignment through module.paths' is part of the standard library types but has no scriptc lowering yet

  4 | module.id = 'replacement';
  5 | module.paths[0] = '/tmp/node_modules';
    | ^~~~~~~~~~~~~~~
  6 | module.paths.push('/tmp/node_modules');

  hint: module.paths is introspection-only in a fixed compiled graph; configure resolution at build time

cjs-module-mutation/main.ts:6:1 - error SC2020: 'module.paths.push()' is part of the standard library types but has no scriptc lowering yet

  5 | module.paths[0] = '/tmp/node_modules';
  6 | module.paths.push('/tmp/node_modules');
    | ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  7 | require.cache[__filename] = module;

  hint: module.paths is introspection-only in a fixed compiled graph; configure resolution at build time

cjs-module-mutation/main.ts:7:1 - error SC2020: 'assignment through require.cache' is part of the standard library types but has no scriptc lowering yet

  6 | module.paths.push('/tmp/node_modules');
  7 | require.cache[__filename] = module;
    | ^~~~~~~~~~~~~~~~~~~~~~~~~
  8 | delete require.cache[__filename];

  hint: a compiled binary has a fixed module graph; cache lookup and Object.keys(require.cache) are read-only

cjs-module-mutation/main.ts:8:8 - error SC2020: 'delete through require.cache' is part of the standard library types but has no scriptc lowering yet

  7 | require.cache[__filename] = module;
  8 | delete require.cache[__filename];
    |        ^~~~~~~~~~~~~~~~~~~~~~~~~
  9 | 

  hint: a compiled binary has a fixed module graph; cache lookup and Object.keys(require.cache) are read-only