import-meta-bare.ts:1:7 - error SC2020: 'ImportMeta' is part of the standard library types but has no scriptc lowering yet

  1 | const bare = import.meta;
    |       ^~~~
  2 | console.log(bare);

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

import-meta-bare.ts:1:14 - error SC1090: 'import.meta' (module-loader metadata has no equivalent in a compiled binary) is not supported yet

  1 | const bare = import.meta;
    |              ^~~~~~~~~~~
  2 | console.log(bare);

import-meta-bare.ts:2:13 - error SC2004: uses of 'bare' inherit the blocker on its declaration

  1 | const bare = import.meta;
  2 | console.log(bare);
    |             ^~~~
  3 | console.log("unreachable");

  hint: the declaration of 'bare' did not compile — fix the diagnostic reported there and these sites clear with it